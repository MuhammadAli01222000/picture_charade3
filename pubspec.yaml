name: picture_charade3
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  lottie: ^3.3.1
  just_audio: ^0.9.46
  google_fonts: ^6.2.1
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8

dev_dependencies:

  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
flutter:

  uses-material-design: true

  assets:
   - assets/images/images_card/
   - assets/images/
   - assets/animation/splash_pages.json
   - assets/json/data.json